# Build-loop task board (the orchestrator reads + rewrites this every tick)

Protocol: `.claude/skills/build-loop/SKILL.md`. Deadline Mon Sep 14 2026 5:00pm PT — submit by 3:00pm PT.

Status: `READY` · `RUNNING` · `REVIEW` · `DONE` · `BLOCKED(<why>)` · `HUMAN` (never auto-run)
Two tasks may run at once only if their **files** columns don't overlap. Lane sessions: docs/LANES.md (`RUNNING@<session>`).

## Human gates (the loop cannot do these — they unblock everything tagged `needs:`)
| id | status | what | unblocks |
|---|---|---|---|
| H1 | CLEARED 2026-09-13 | AWS creds: `default` profile via `aws login` (the team AWS account, root session — expires; re-run `aws login` if calls start failing). Bedrock model access verified by T8 | T8, T9, T10, T11 |
| H2 | ANSWERED | Frontend = Nash's static UI on origin/main (root: index.html, styles.css, pathways.js, agent.js, app.js). It is a MOCK: never calls /reason, fabricates dates/regulators. Nash is actively pushing to main | T12, T20 |
| H3 | HUMAN | Push access: user chose KJprogrammer, but it has pull-only on youssefnashat/Credential-Bridge — youssefnashat must add KJprogrammer as collaborator (Write) | T13 |
| H5 | CLEARED — Anthropic use-case form submitted via console; Claude Sonnet 4.6 live on Bedrock us-west-2 (Sonnet 5 not offered to this account) | Bedrock is denied for the team AWS account: "Your account is currently being verified" (typically <2h; if longer email aws-verification@amazon.com), then Bedrock console us-west-2 → Model access → Claude → submit Anthropic first-use form / accept agreement (agreementAvailability is NOT_AVAILABLE). Claude 3.7 Sonnet is end-of-life. Fallback: T22 (Anthropic API key) | T8, T9, T10, T11b |
| H6 | HUMAN (recommended) | Running as the AWS ROOT user. Create an IAM user/role for Bedrock + AgentCore before T10 deploys | T10 |
| H7 | HUMAN (only (c),(d) remain; (a),(b) researched in T24) | From T7: (a) CNO now names approved ECA providers, not NNAS — is `gateway: NNAS` still right for CA-ON RN? (b) CA BRN English requirement is "reasonable doubt" only, no TOEFL named — fix CA-US RN req 2? (c) PEO min experience may have dropped 48→24 months on 2026-07-01 (Cloudflare blocks fetch) (d) GB registry: use GOV.UK Regulated Professions Register? | kb accuracy |
| H8 | HUMAN (fallback) | ANTHROPIC_API_KEY for the T22 fallback: put in backend/.env with CREDBRIDGE_PROVIDER=anthropic, CREDBRIDGE_MODEL=claude-sonnet-5 (never committed) | T8/T9 if H5 is slow |
| H4 | HUMAN | AWS Builder ID, record + upload YouTube video, publish builder.aws posts, submit Devpost | — |

## Auto tasks
| id | pri | status | owner | files | task | done-when |
|---|---|---|---|---|---|---|
| T1 | P0 | DONE | grounding-engineer | backend/app/reference.py, backend/tests_smoke.py | Fix `_region_key`: non-CA countries all map to `US-*` (Germany→`US-GE`), so DE/GB/AU rulesets are unreachable from natural profiles | every kb/store ruleset resolves `source:"kb"` from a natural profile; smoke passes |
| T2 | P0 | DONE | orchestrator | .claude/settings.json, .claude/hooks/** | Push gate never blocks (bad matcher; smoke fails with exit 1, hooks block only on 2) | verified: non-push exit 0 · healthy push exit 0 · broken repo push exit 2 |
| T3 | P0 | DONE | strands-engineer | backend/app/agent.py, backend/app/agents/**, backend/app/orchestration/case_graph.py | Verify every Strands call against the INSTALLED SDK (structured_output, BedrockModel params, GraphBuilder edge-condition signature, harvester fetch-tool import); fix mismatches; add no-AWS construction test | build_agent(), build_harvester(), build_case_graph() construct offline; smoke passes |
| T4 | P1 | READY(queued@aws-hackathon-aa after T14; + api.py audit log path is cwd-relative too) | api-deploy-engineer | backend/app/orchestration/session_store.py, backend/app/orchestration/orchestrator.py, backend/app/api.py, backend/tests_smoke.py | session_store writes to cwd-relative `data/sessions` (breaks under AgentCore / other cwd); prior-steps seed read outside the session lock; smoke test pollutes real session dir | paths repo-anchored or env-set; seed inside lock; smoke uses temp dir |
| T5 | P1 | DONE | submission-pm | README.md, docs/*.md (not TASKS/CONTEXT_LOG), docs/architecture.png, .claude/skills/hackathon-submission/** | Reconcile docs with verified Devpost rules; skill's video beats (harvester/checker/packet) contradict VIDEO_SCRIPT.md; README arch block omits KB | every README claim reproducible by a documented command; one consistent video plan |
| T6 | P1 | DONE(in T5) | submission-pm | docs/architecture.png, docs/ARCHITECTURE.md | Render architecture diagram to PNG (Devpost upload) | PNG exists and matches code |
| T7 | P2 | DONE | grounding-engineer | kb/store/**, kb/sources/**, docs/evidence/url-check.md | Check every source URL in KB + regulators.json resolves; dead → `needs_review:true` + note. Never invent replacements | report of URL status committed in docs/evidence/url-check.md |
| T8 | P0 | DONE | strands-engineer | backend/.env, backend/.env.example, backend/app/agent.py, backend/app/agents/harvester.py, backend/tests_offline.py | Confirm the model ID is invocable in us-west-2 (`aws bedrock list-inference-profiles`); Claude 3.7 Sonnet may be legacy — switch to a current Claude on Bedrock if not | one live build_pathway returns a valid contract |
| T9 | P0 | DONE | qa-verifier | docs/evidence/** | Record `git rev-parse HEAD` beside the evidence (sets come from the live KB). Run the 3 README curls + simulate_rejection + reset; save outputs; score grounding (every sourceUrl present in KB) → the README accuracy line | docs/evidence/*.json + accuracy number |
| T10 | P1 | BLOCKED(T8,T14) | api-deploy-engineer | backend/** deploy files | `agentcore launch` (time-box 2h). NOTE: Runtime invoke needs SigV4 (or a JWT inbound authorizer) — a static page can't call it; keep browser → FastAPI /reason, show Runtime via `agentcore invoke` (or FastAPI proxying to Runtime) | Runtime URL invokes, or failure logged in CONTEXT_LOG |
| T11 | P2 | T11a DONE · T11b harvest BLOCKED(T8/H5); US-TX queue URL may be the wrong page — hold | grounding-engineer | kb/pipeline/**, then kb/store/** | `run_harvest_batch.py --limit 3`, then human-review diffs | new rulesets validate, needs_review=true |
| T12 | P0 | DONE | frontend | index.html, styles.css, pathways.js, agent.js, app.js | Adapter: map FE profile/event → /reason contract (Physician normalisation, currentSteps), render backend steps + sourceUrl links + regulator header, async + loading/error, ?api= endpoint, mock only behind ?mock=1. Honesty pass: nothing fabricated on screen when live | all 4 events render from the live backend; no fabricated data visible |
| T13 | P0 | BLOCKED(H3) | orchestrator | — | Push commits | remote matches local |
| T14 | P1 | RUNNING@orchestrator (aa stood down) | api-deploy-engineer | .claude/skills/agentcore-deploy/** | Verify the real AgentCore deploy toolchain for bedrock-agentcore 1.23 (README says the `agentcore` CLI replaces the starter toolkit yet installs the toolkit). Install in a SCRATCH venv, not backend/.venv. Rewrite the skill as a verified runbook | `agentcore --help` output captured; every runbook command matches it |
| T15 | P0 | RUNNING@aws-hackathon-78 | frontend | none (read-only) | Locate the frontend that calls `getAgentReasoning()`: search local disk and the GitHub accounts (KJprogrammer, kjhattu-roadpost, youssefnashat). If none exists, report that plus what a minimal static UI would need | path/repo reported, or "not found" with where you looked |
| T16 | P1 | DONE | qa-verifier | backend/eval_run.py, docs/evidence/eval-* | Build the eval harness for T9: run the scenario set against a base URL and score contract validity, grounding (every sourceUrl in the KB/regulators.json URL set), delay semantics (flag + at-risk + ISO date), unregulated-SWE judgment. Test offline against a stub | passes against a stub server; prints the one-line README accuracy summary |
| T17 | P1 | DONE | api-deploy-engineer | backend/requirements.txt, backend/app/orchestration/orchestrator.py (docstring) | From T3/T11a/T22: pin `strands-agents[web-fetch,anthropic]==1.55.1` + `jsonschema` + `botocore[crt]` (REQUIRED: `aws login` creds need awscrt — without it every Python Bedrock call fails; installed in .venv 2026-09-13, verified sts works) (KB validation silently degrades to required-keys without it) (structured_output_model, vended_tools, context-aware edge conditions don't exist in 1.0); drop unused strands-agents-tools; fix orchestrator docstring ("Agent is stateless per-call" is false — reason() builds a fresh agent per call) | fresh venv from requirements.txt passes tests_offline + tests_smoke |
| T18 | P2 | HUMAN(decide) | api-deploy-engineer | backend/app/api.py | Optional: expose the approval-gated case graph (pathway → human approval → finalize) as an endpoint so the multi-agent story is visible in the demo. Scope call for the humans — adds surface area before the deadline | endpoint + offline test |
| T19 | P1 | RUNNING@aws-hackathon-d2 (with T25) | orchestrator + submission-pm | CLAUDE.md (orchestrator), docs/SETUP.md, README.md, docs/blog/** (submission) | Claude 3.7 Sonnet is unavailable on Bedrock for this account (agreement NOT_AVAILABLE, no inference profile). Replace every "claude-3-7-sonnet" / "Claude 3.7 Sonnet" mention with the model T8 verifies; drop the stale "(verified)" claim | `grep -ri "3-7-sonnet\|3.7 sonnet"` returns nothing outside git history |
| T20 | P0 | DONE | orchestrator | README.md + Nash's 5 root files | Merge origin/main (Nash: 0b443ed frontend, f366e00 frontend-only README) into local. Keep project README; fold frontend run instructions into a Frontend section | merge committed; smoke + offline tests pass; frontend files present |
| T21 | P2 | DONE | frontend | pathways.js | Add United Kingdom / Australia / Germany targets so the KB's national rulesets are reachable from the UI | selecting each yields a KB-grounded pathway |
| T22 | P0 | DONE | strands-engineer | backend/app/agent.py, backend/app/agents/harvester.py, backend/tests_offline.py, backend/.env.example | Provider fallback: CREDBRIDGE_PROVIDER=bedrock (default) or anthropic (Strands Anthropic provider, ANTHROPIC_API_KEY) so the demo can run if Bedrock isn't ready | offline test constructs both providers; strands version unchanged |
| T23 | P0 | DONE (entrypoint .env left to aa after T14) | api-deploy-engineer | backend/app/api.py, backend/run_local.sh | Nothing loads backend/.env (python-dotenv installed, unused) — CREDBRIDGE_MODEL/AWS_REGION/CREDBRIDGE_TODAY in .env are ignored. load_dotenv(backend/.env) BEFORE importing app.agent (MODEL_ID is read at import) | server started from any cwd picks up .env values (offline check) |
| T24 | P1 | DONE | grounding-engineer | backend/reference/regulators.json | 7 dead URLs remain in the compact fallback (CNO, NMC, BRN, CRNA, Texas BON, NYSED eng/teacher); apply the same-domain replacements listed in docs/evidence/url-check.md, verifying each | curl 200/verified for every regulators.json URL; smoke passes |
| T25 | P1 | RUNNING@aws-hackathon-d2 | submission-pm | docs/ORCHESTRATION.md, docs/VIDEO_SCRIPT.md, README.md, docs/DEVPOST.md, docs/blog/**, docs/architecture.png | T5 QA follow-ups: README:158 cite url-check.md; frontend 'fixed contract' claims (DEVPOST:49, blog 03:6) are false — it's a mock until T12; README:28 overclaim; DEVPOST temperature; re-render PNG taller (-w 2400/TB) after T8 model. Plus post-T3 refresh: shared-agent concurrency risk is FIXED (65777ca) — un-gate video beat 8, fix ORCHESTRATION.md "stateless per call"/MultiAgentPlugin claims; frontend disclosure (Nash's UI, built Sep 13 = within period) | docs match committed code |
| T26 | P2 | DONE | grounding-engineer | kb/pipeline/*.py | From T11a/T22 QA: (0) run_harvest*.py must load_dotenv(backend/.env) BEFORE importing app.agents.harvester; (1) write_harvested/get accept `../../x` or absolute jurisdiction → reject non-`^[A-Z]{2}(-[A-Z0-9]{1,3})?$` or check resolve() stays under KB_ROOT; (2) drop self-certified `last_verified` on agent writes; (3) warn loudly when jsonschema is missing | hostile jurisdiction refused; offline test |
| T27 | P0 | DONE | strands-engineer | backend/app/agent.py, backend/.env.example, backend/tests_offline.py | From T22 QA: Bedrock MODEL_ID/REGION read at import (agent.py:22-23) while provider is read at call time → late .env silently keeps the EOL 3.7 id; read all model env at call time; fail fast (ValueError) when provider=anthropic and no ANTHROPIC_API_KEY; comment out the Bedrock-format CREDBRIDGE_MODEL default in .env.example | offline: env set after import is honoured on both providers; missing key fails at build |
| T28 | P1 | DONE | grounding-engineer | kb/store/CA-ON/registered-nurse.json | Demo persona gap: CNO guide lists Evidence of Practice (practised in past 3 yrs), Transition to Practice, Jurisprudence Exam, citizenship/immigration status — missing from CA-ON RN ruleset. Add only what the fetched CNO page supports | ruleset validates; each new requirement cites a CNO page |
| H9 | HUMAN | Team name spelling in README: "Youssef Nashaat" vs handle youssefnashat — confirm before submit | — |
| T29 | P0 | DONE | strands-engineer | backend/app/agent.py, backend/tests_offline.py | LOSS-GUARD #2 GAP (T16 QA probe G): response `regulator`/`regulatorUrl` are free text from the model — nothing grounds them; an invented board + URL scored as a pass. After reason(): set regulator/regulatorUrl deterministically from lookup() (null if unregulated/unknown), and null any step sourceUrl not in that lookup's URL set (log it). Grounding stays in code, decisions in the model | offline: fabricated regulatorUrl/sourceUrl from a stub model never reach the response |
| T30 | P1 | DONE | qa-verifier | backend/eval_run.py | From T16 QA: grade regulatorUrl against the per-target set; label "off-ruleset" (not off-jurisdiction) when the set is empty/different profession; compute sourced% only over contract-valid responses; fix "n/a" warning text | self-test catches a fabricated regulatorUrl |
| T31 | P2 | READY(after T29/T30 commit) | grounding-engineer | backend/app/reference.py (+ callers in agent.py, eval_run.py) | One public `reference.grounding_urls(profession, country, region)` + `norm_url` used by both agent._ground and eval_run, so the "what counts as grounded" rule lives in one place (today duplicated; agent imports private _compact) | agent and eval import the same function; offline + self-test pass |
