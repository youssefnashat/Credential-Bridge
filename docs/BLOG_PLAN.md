# builder.aws posts (+0.2 pts each, max 0.6). Every title starts "Agents for Humans:". Publish before the deadline (Sep 14 2026, 5:00pm PDT).

| # | draft | title |
|---|---|---|
| 1 | `docs/blog/01-grounded-reasoning.md` | Agents for Humans: grounding a Strands agent so it doesn't invent licensing steps |
| 2 | `docs/blog/02-visible-judgment.md` | Agents for Humans: make the agent's judgment the demo, not the plumbing |
| 3 | `docs/blog/03-two-runtimes.md` | Agents for Humans: one Strands agent, two runtimes (FastAPI locally, AgentCore Runtime) |

## Before publishing each post
- Resolve every `TODO(team)` in the draft (live results, deploy status).
- Any agent output quoted in a post must be copied from a saved run in `docs/evidence/`; otherwise keep the "illustrative" label.
- No numbers (accuracy, latency, step counts) unless they come from `docs/evidence/`.
- Link the repo and the video at the end of each post; add the published links to `docs/DEVPOST.md`.
