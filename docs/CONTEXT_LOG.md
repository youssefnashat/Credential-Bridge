# Context log (append; newest at bottom)
- 2026-09-13 Backend built to the /reason contract with REAL Strands reasoning grounded on backend/reference/regulators.json (5 professions x CA/US regions + unregulated SWE case). FastAPI + AgentCore entrypoint on the agentcore CLI. Model us.anthropic.claude-3-7-sonnet on us-west-2 (verified). No-AWS smoke test passes. NOT yet run against Bedrock — first real run is the curl build_pathway. Structured-output fallback (text+parse) wired in case the SDK version raises.
- 2026-09-13 Added compliance KB (kb/): schema + source registry + curated rulesets for CA-ON (nurse, civil eng), US-NY, GB, AU (nurse). kb/pipeline validates all + builds _index.json (6 rulesets / 5 jurisdictions, no AWS). Reasoner now grounds on kb/store first, regulators.json fallback. Specialized agents package added: harvester (KB pipeline), watcher (dormant), pathway (reasoner). CLAUDE.md retuned to SPRINT mode with 3 loss-guards + smoke-test gate on git push. Three builder.aws posts drafted in docs/blog. SETUP.md written for Claude-Code-in-VSC. NOT yet run against Bedrock — first live call is the build_pathway curl.
- 2026-09-13 Expanded KB to 7 jurisdictions (added DE with full Anerkennung/B2/Fachsprachprüfung path, CA-BC with NCAS note, US-CA). Added ontology (kb/ontology/credential-ontology.md): entities, valid_at semantics, cross-jurisdiction equivalences (NNAS≈CGFNS≈AHPRA≈Anerkennung; NCLEX vs CBT+OSCE vs Kenntnisprüfung), 4 conflict types. Built ORCHESTRATION: session_store (per-session locks + atomic writes), Orchestrator (shared agent + bounded ThreadPool, truly-parallel sessions), case_graph (Strands GraphBuilder pathway->approval->finalize). New endpoints /sessions/{id}/reason, /batch, GET /sessions. demo_concurrent.py fires 5 caseworker sessions at once. Added harvest_queue + run_harvest_batch for unattended KB expansion. Smoke test now covers orchestration (skips if strands absent). Referenced Strands samples repo + MultiAgentPlugin/session-persistence (merged to main) for the concurrency design. Still not run against Bedrock here — first live calls are the curls + demo_concurrent.
- 2026-09-13 BUILD LOOP started. Local git linked to github.com/youssefnashat/Credential-Bridge (origin/main, NOT pushed). backend/.venv: strands-agents 1.55.1, tools 0.8.8, bedrock-agentcore 1.23.0. Added docs/TASKS.md (board), .claude/skills/build-loop (protocol), .claude/agents/qa-verifier (gate). Fixed push gate (T2): old matcher `Bash(git push*)` never matched and exit 1 never blocks; now .claude/hooks/pre_push_gate.sh exits 2 on smoke/KB failure. Found: _region_key maps every non-CA country to US-* (DE unreachable) → T1. Running: T1 grounding, T3 strands SDK verification, T5 submission docs. No AWS creds on this machine → all live tasks BLOCKED(H1). Devpost rules verified: Strands required, AgentCore optional (+tech score), video ≤5min public, Builder ID, Apache/MIT, deadline Sep 14 5pm PDT.
- 2026-09-13 T1 DONE (QA PASS): _region_key maps country aliases → CA/US/GB/AU/DE/IE; subdivisions only for CA/US; unknown country → unknown_region (never US-*). All 8 KB rulesets + 20 compact keys + all demo_concurrent profiles resolve; path-traversal probes stay inside kb/store. Minor follow-ups: `UNKNOWN:` colon (Windows-only), US states missing from _SUB. LANES introduced (docs/LANES.md): 6 peer sessions each own a file lane; orchestrator aws-hackathon-6a is sole committer. User wants GitHub pushes from KJprogrammer (currently pull-only on the repo). No AWS profiles exist on this machine yet.
- 2026-09-13 H1 CLEARED: user ran `aws login` in another terminal → `default` profile, the team AWS account (root login session; default region us-east-1, project uses us-west-2 via backend/.env). T8 (model verification) dispatched to strands lane aws-hackathon-09. T3 in QA (fresh Agent per call fixes history leak + ConcurrencyException; BedrockModel params were silently dropped). Added T17 (requirements pin), T18 (graph endpoint — human decision).
- 2026-09-13 T15 (frontend lane): frontend found on origin/main — Nash pushed a static vanilla-JS UI (0b443ed) + frontend-only README (f366e00) after our fetch; local and origin diverged (merge-base 79b2072, only README overlaps). UI is strong but is a MOCK: agent.js never calls /reason, fabricates dates/queue lengths, pathways.js hardcodes regulators; FE contract differs from schemas.py in every field. Plan: keep UI at root, adapter in agent.js (T12), merge after T5 (T20). Claude 3.7 Sonnet confirmed unavailable on Bedrock (agreement NOT_AVAILABLE, no inference profile) → T8 switches model, T19 cleans docs. T11a (pipeline validate-before-write) started by grounding lane.
- 2026-09-13 T3 DONE (QA PASS, commit 65777ca). T8 live probe: 3.7 Sonnet end-of-life; every current Claude on Bedrock → AccessDenied "account is currently being verified"; agreement NOT_AVAILABLE; 0 billed calls. New human gate H5 (critical path). T22 started: Strands provider switch (bedrock|anthropic) as a filming fallback. Root-user use flagged (H6).
